from typing import *

from BNGame import BNCellStatus


def odds_AI(board: List[List[BNCellStatus]]) -> Tuple[int, int]:
    """Computes the odds of having a ship cell on every position of the board, then shoots the highest one.

    Args:
        board (List[List[BNCellStatus]]): The board of the game.

    Returns:
        Tuple[int, int]: The position with the higher odds.
    """
    # TODO: implement
    return -1, -1
